{{each data}}
<tr>
    <td>{{$index+1}}</td>
    <td>{{$value.username}}</td>
    <td>
        {{$value.group}}
    </td>
    <td><button class="btn btn-danger remove-user" data-id="{{$value._id}}">删除</button></td>
</tr>
{{/each}}